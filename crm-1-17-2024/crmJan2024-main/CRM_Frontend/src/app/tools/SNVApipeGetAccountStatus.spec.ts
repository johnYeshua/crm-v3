import { SNVApipeGetAccountStatus } from './SNVApipeGetAccountStatus';

describe('SNVApipeGetAccountStatus', () => {
  it('should create an instance', () => {
    expect(new SNVApipeGetAccountStatus()).toBeTruthy();
  });
});
