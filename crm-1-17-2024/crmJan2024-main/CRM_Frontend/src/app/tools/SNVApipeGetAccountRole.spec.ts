import { SNVApipeGetAccountRole } from './SNVApipeGetAccountRole';

describe('SNVApipeGetAccountRole', () => {
  it('should create an instance', () => {
    expect(new SNVApipeGetAccountRole()).toBeTruthy();
  });
});
